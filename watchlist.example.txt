# Swing scanner watchlist (example)
# - One symbol per line.
# - Empty lines and lines starting with '#' are ignored.
# - Inline '#' comments after a symbol are stripped.
# - Symbol formats are provider-specific:
#     yfinance: NSE needs the ".NS" suffix (e.g. RELIANCE.NS); US tickers bare (AAPL).
#     dhan:     bare NSE ticker (e.g. RELIANCE) — security ids are mapped internally.
#     finnhub:  same as yfinance for NSE; US bare; both require a paid plan for candles.

# --- NSE large caps (yfinance / finnhub) ---
RELIANCE.NS
INFY.NS
TCS.NS
HDFCBANK.NS
ICICIBANK.NS
SBIN.NS
LT.NS
AXISBANK.NS

# --- US large caps ---
AAPL
MSFT
NVDA
GOOGL
AMZN
META
